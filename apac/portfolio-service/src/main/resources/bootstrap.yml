spring:
#  profiles.active: local
  application:
     name: portfolio-service
  cloud:
    config:
      uri: ${CROSSPLANE_EP}
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}
