spring:
#  profiles.active: local
  application:
     name: portfolio-service
  cloud:
    config:
      uri: http://crossplane-service
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}
